export const setMagFilterAction = (value) => (
    {
        type: 'SET_MAG_FILTER',
        value,
    }
);

export const setMagTypeFilterAction = (value) => (
    {
        type: 'SET_MAGTYPE_FILTER',
        value,
    }
);